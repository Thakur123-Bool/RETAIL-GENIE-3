# backend/app/requirements.txt
Flask==2.3.3
flask-cors==3.0.10
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0

# authentication & Azure SDK (msal bumped to satisfy azure-identity)
msal==1.24.0
azure-identity==1.15.0
azure-storage-file-datalake==12.7.0
azure-core==1.30.0

# Data/ML libs (remove or trim if you don't need them in production)
pandas==2.1.4
pyarrow==14.0.1
scikit-learn==1.3.2

# Optional; if deltalake installation fails you can remove it and rely on parquet fallback
deltalake==0.8.0

# Kaggle for dataset imports (needs build tools)
kaggle==1.5.13

tabulate==0.9.0
